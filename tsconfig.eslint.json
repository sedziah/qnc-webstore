{
  "extends": "./tsconfig.json", // Extends the main TypeScript config file
  "include": [
    "src/**/*.ts", // Include all TypeScript files in src for linting
    "src/**/*.tsx" // Include all TypeScript React files
  ],
  "exclude": [
    "node_modules", // Exclude node_modules
    "**/*.spec.ts" // Exclude test files
  ]
}
